const initialState = {
    cards: [],
    loadingd: false,
    currentPage:1,
    cardsPerPage: 9,
  };

export function Reducer (state = initialState, action){
    switch (action.type) {
        case "":
            return{
                
            }
        case "":
            return {
             
            }
        case "":
            return{
            
            }
        case "":

        default: return state
    }
}